pipeline {
 agent {label 'jenkins-slave'}
 options {
  skipDefaultCheckout()
 }
stages {
    stage('SCM') {
      steps {
        echo "entered into SCM Stage"
        sh "cd /var/jenkins_home"
        checkout scm
      }
    }
  }
}
